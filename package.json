{
  "dependencies": {
    "axios": "^1.7.7"
  }
}
{
  "name": "hospital-prediction-monorepo",
  "version": "1.0.0",
  "dependencies": {
    "axios": "^1.7.7"
  },
  "scripts": {
    "start:frontend": "cd frontend && npm install && npm start",
    "start:hospital-prediction": "cd backend && npm install && npm start",
    "start": "concurrently \"npm run start:frontend\" \"npm run start:backend\""
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}

